<%=render "partials/navigation"%>
<%suma = 0%>
<%mes=1%>
<%año1=1%>
<%año=Time.now.year%>
</p>  <div class="panel panel-primary">
  <table class="table">
    <thead>
      <tr>
      <th>Nombre:</th>
      <th>Apellido:</th>
      <th>Pago:</th>
    </tr>
  </thead>
  <tbody>
<% Pay.all.each do |t| %>
<%mes=t.created_at.strftime"%m"%>
<%año1=t.created_at.strftime"%Y"%>
<%=año%>
<%=año1%>
<% if (mes=='08')%>
<%if (año1.ecuals(año))%>
  <tr>
  <td><%= t.parent.nombre %></td>
  <td><%= t.parent.apellido %></td>
  <td><%= t.monto %></td>
  <%suma = suma + t.monto%>
    </tr>
<%end%>
<%end%>
<%end%>
</tbody>
</table>
<table class="table">
  <thead>
    <tr>
    <th>Nombre:</th>
    <th>Apellido:</th>
    <th>Sueldo:</th>
  </tr>
  </thead>
<tbody>
<% Salary.all.each do |t| %>
<%mes=t.created_at.strftime"%m"%>
<% if (mes=='08')%>
<tr>
<td><%= t.worker.nombre %></td>
<td><%= t.worker.apellido %></td>
<td><%= t.monto %></td>
<%suma = suma - t.monto%>
</tr>
<%end%>
<%end%>
</tbody>
</table>
<table class="table">
  <thead>
    <tr>
    <th>Motivo:</th>
    <th>Furgon:</th>
    <th>monto:</th>
  </tr>
  </thead>
<tbody>
<% Speding.all.each do |t| %>
<%mes=t.created_at.strftime"%m"%>
<% if (mes=='08')%>
<tr>
<td><%= t.nombre %></td>
<td><%= t.van.patente %></td>
<td><%= t.monto %></td>
<%suma = suma - t.monto%>
</tr>
<%end%>
<%end%>
</tbody>
</table>
<table class="table">
<thead>
  <tr>
  <th>La ganancia Total, descontando los Sueldos y los gastos es de: $ <%=suma%>, <%=año1%></th>
</tr>
</thead>
</table>
</div>
<p>
  <%= link_to 'Atrás',welcome_index_path,class:"btn btn-default btn-lg active btn-sm "%>
</p>
