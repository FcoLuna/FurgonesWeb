<%= form_for(@speding) do |f| %>
  <% if @speding.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@speding.errors.count, "error") %> prohibited this speding from being saved:</h2>

      <ul>
      <% @speding.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :"Motivo" %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :monto %><br>
    <%= f.number_field :monto %>
  </div>
  <%= f.label :"Furgon" %>
  <div class="controls">
     <%= collection_select( :speding, :van_id, Van.all, :id, :patente, {}, { :multiple => false } ) %>
   </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
